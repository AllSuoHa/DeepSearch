# DeepSearch — AI 深度搜索引擎 · 产品需求文档

> **文档性质**：这是一份产品需求描述。它告诉你"我要什么"和"为什么"，不告诉你"怎么写代码"。架构设计、技术选型、代码结构、实现方案由你（开发者）自主决策。如果你认为有更好的方案，可以调整，但请在开发笔记中说明理由。

> **实现状态（2026-08-28）**：本文保留为原始需求历史基线，不代表当前目录结构。项目当前版本为 **2.1.0**，F1–F16 均已有可运行实现，并新增结论优先报告、证据感知追问、结构化研究要求和自主研究任务；最近一次回归为 **24 项测试全部通过**。逐项代码与测试证据见 [`docs/REQUIREMENTS_TRACEABILITY.md`](docs/REQUIREMENTS_TRACEABILITY.md)，当前架构见 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)。

> **版本口径说明**：原需求把“知识积累”称为 V2；实际项目将架构重构、质量评分和 Web 工作台定义为 2.0.0，将报告综合与自主任务定义为 2.1.0。知识积累与跨报告关联仍属于后续路线。下文保留原始表述以便追溯，当前事实以 `README.md` 和 `docs/` 为准。

---

## 一、项目是什么

### 一句话

**我问一个问题，它帮我搜、帮我读、帮我验证、帮我写成报告，我只需要看结果。**

### 它不是什么

- 不是搜索引擎（不返回一堆链接让我自己看）
- 不是聊天机器人（不是闲聊，是严肃的信息获取）
- 不是 RAG 问答（不是从固定知识库里检索，是实时去互联网获取最新信息）

### 它和普通搜索的本质区别

| | 普通搜索 | DeepSearch |
|---|---|---|
| 搜索方式 | 搜一次就完 | **迭代式搜索**：搜→评估→不够→调整策略→再搜 |
| 信息深度 | 只看摘要 | **抓取网页正文**，深度阅读 |
| 信息验证 | 无 | **多源交叉验证**，标注可信度 |
| 输出 | 链接列表 | **结构化报告**，每个论点带引用溯源 |
| 智能程度 | 固定流程 | **自主决策**：判断信息够不够、策略要不要调整 |

### 核心设计理念

**这不是一个 Pipeline（流水线），这是一个 Agent（智能体）。**

区别在于：

- Pipeline：搜索→抓取→整理→生成，固定流程，一条路走到黑
- Agent：搜索→**评估信息够不够**→不够就调整策略再搜→**检查回答质量**→不合格就修正→输出

系统要有"自己想、自己判断、自己调整"的能力。

---

## 二、用户故事（请代入这些场景来理解需求）

### 故事 1：临时深度调研（最核心场景）

我是一个技术人员，想了解"2025年主流Agent框架有哪些，各自优劣是什么"。

我打开终端，输入这个问题。

我期望看到：

1. 系统先告诉我"正在分析问题，拆解为3个子问题..."
2. 然后"正在搜索第1轮...找到8个相关来源"
3. 然后"正在抓取网页正文...成功6个，失败2个"
4. 如果信息不够，系统说"信息不够全面，正在补充搜索..."
5. 然后"正在生成报告..."
6. 最终我看到一份结构化报告：
   - 有总览
   - 有各框架详细介绍
   - 有对比表格
   - 每个论点标注了来源 [1][2][3]
   - 矛盾信息被明确指出
7. 报告自动保存为本地 Markdown 文件

整个过程我能看到进度，不是干等。

### 故事 2：追问细节

看完报告后，我想追问："你提到 LangGraph 支持多智能体协作，具体是怎么实现的？"

系统基于已有报告内容回答。如果已有信息不够，它会自动追加搜索，然后整合回答。

### 故事 3：每日信息收集（附加场景）

我关心"大模型行业动态"。我配置了每天早上 8:00 自动执行。

到时间后，系统自动：

1. 搜索最近24小时的相关信息
2. 抓取、整理、交叉验证
3. 生成一份日报，保存到 ./reports/ 目录
4. 终端提示我"今日报告已生成"

我打开文件直接看。

### 故事 4：简单快速查询

我只想知道"Python 3.13 什么时候发布"。

系统识别这是一个简单事实查询：

- 不需要拆解
- 不需要多轮迭代
- 搜一次，找到答案，快速返回
- 5-15秒搞定

---

## 三、功能优先级

### 🔴 P0 — 核心功能（必须完善，这是项目的灵魂）

| 编号 | 功能 | 描述 | 为什么重要 |
|------|------|------|-----------|
| F1 | **问题理解与策略制定** | 理解用户意图，判断问题类型（简单事实/对比分析/开放探索），制定对应的搜索策略。复杂问题拆解为子问题，简单问题不拆解 | 这是"智能"的体现，不是所有问题都走同一套流程 |
| F2 | **多路搜索与网页抓取** | 根据策略执行搜索，抓取搜索结果对应的网页正文（不只看摘要）。并发执行，单个失败不阻塞整体 | 信息获取的广度和深度 |
| F3 | **迭代式搜索** | 搜索完一轮后，评估信息是否充分。不充分则调整策略（换关键词、换方向、深入子话题）再搜。最多迭代3轮，有明确停止条件 | **这是区别于普通搜索的核心能力，是"Agent"而非"Pipeline"的关键** |
| F4 | **内容整理与交叉验证** | 去重、归纳、交叉验证。多源提到→高可信；单一来源→标注待验证；来源矛盾→明确指出 | 信息质量保障，不是照搬搜索结果 |
| F5 | **生成带引用的结构化回答** | 用大模型生成结构化报告。每个论点标注引用 [1][2][3]，引用对应具体来源（标题+URL）。格式清晰可读 | **这是最终交付物，质量决定项目成败** |
| F6 | **答案自校验** | 生成回答后，检查：引用是否真实对应源文档、是否覆盖了问题的所有方面。不合格则修正 | 不是生成完就给用户，有质量把关 |
| F7 | **报告持久化** | 每次生成的回答保存为本地 Markdown 文件，文件名有意义（含日期和主题），方便日后查阅 | 不是看完就没了 |
| F8 | **交互入口** | 提供命令行界面（CLI），用户输入问题，看到完整的搜索-生成过程，最终输出回答。全程有进度提示 | 用户与系统的交互方式 |
| F9 | **流式输出与进度可见** | 用户输入后3秒内必须有第一个反馈。全程显示当前在做什么。最终回答逐字/逐段输出 | 不是性能问题，是体验问题 |

### 🟡 P1 — 重要附加功能

| 编号 | 功能 | 描述 |
|------|------|------|
| F10 | **多轮追问** | 用户看完回答后可以追问。系统基于已有内容回答，必要时追加搜索 |
| F11 | **搜索策略自适应** | 根据第一轮搜索结果动态调整：结果太少→扩大范围；结果太泛→加限定词；发现新子话题→追加搜索 |
| F12 | **可信度标注** | 每条信息标注：✅ 多源验证 / ⚠️ 单一来源 / ❌ 来源矛盾 |
| F13 | **搜索过程可观测** | 用户能看到系统"在想什么"：搜了什么、为什么调整策略、找到了什么、为什么认为信息够了 |

### 🟢 P2 — 个人扩展功能

| 编号 | 功能 | 描述 |
|------|------|------|
| F14 | **定时自动收集** | 配置关注主题和执行时间，定时自动执行深度搜索，生成日报 |
| F15 | **主题管理** | 添加/删除/修改关注主题，配置持久化为本地文件 |
| F16 | **历史查阅** | 查看历史报告列表，按日期或主题检索 |

### ⚪ 明确不需要

| 内容 | 原因 |
|------|------|
| 用户注册/登录 | 个人工具，单用户 |
| 数据库 | 本地文件存储足够 |
| 微服务/分布式 | 单机运行 |
| Docker/K8s | 本地直接运行 |
| Web 前端（Vue/React） | CLI 足够，Streamlit 可选但非必须 |
| 重型框架（LangChain/LlamaIndex） | 自己实现核心逻辑，展示能力 |

---

## 四、核心流程

### 4.1 主流程：迭代式深度搜索

    用户输入问题
         │
         ▼
    ┌──────────────────┐
    │  问题理解与规划    │
    │  - 判断问题类型    │
    │  - 制定搜索策略    │
    │  - 复杂问题拆解    │
    └────────┬─────────┘
             ▼
    ┌──────────────────┐
    │  执行搜索         │◄─────────────────────┐
    │  - 多路搜索       │                       │
    │  - 并发抓取正文    │                       │
    │  - 失败跳过       │                       │
    └────────┬─────────┘                       │
             ▼                                 │
    ┌──────────────────┐                       │
    │  信息充分性评估    │                       │
    │  "信息够回答吗？"  │                       │
    └────────┬─────────┘                       │
             │                                 │
        ┌────┴────┐                            │
        │         │                            │
        ▼         ▼                            │
      够了      不够（且未超过3轮）              │
        │         │                            │
        │         ▼                            │
        │    ┌──────────────────┐              │
        │    │ 策略调整          │──────────────┘
        │    │ - 缺什么信息？     │
        │    │ - 换什么关键词？   │
        │    │ - 深入哪个方向？   │
        │    └──────────────────┘
        │
        ▼
    ┌──────────────────┐
    │  内容整理         │
    │  - 去重           │
    │  - 交叉验证       │
    │  - 可信度标注      │
    └────────┬─────────┘
             ▼
    ┌──────────────────┐
    │  生成回答         │
    │  - 结构化报告      │
    │  - 标注引用       │
    │  - 流式输出       │
    └────────┬─────────┘
             ▼
    ┌──────────────────┐
    │  自校验           │
    │  - 引用准确吗？    │──── 不合格 → 修正
    │  - 覆盖完整吗？    │
    └────────┬─────────┘
             ▼
    ┌──────────────────┐
    │  输出与保存       │
    │  - 展示给用户      │
    │  - 保存 Markdown  │
    │  - 等待追问       │
    └──────────────────┘

### 4.2 关键设计说明

#### 关于迭代搜索（F3）

这是整个项目最核心的设计。请注意：

- 不是"搜3轮"，而是"搜到信息够了为止，最多3轮"
- 每轮迭代之间，系统要能说明"为什么需要再搜"（记录原因）
- 简单问题可能1轮就够了，不要过度迭代
- 停止条件：信息充分 / 达到最大轮次 / 连续两轮没有新增有效信息

#### 关于策略自适应（F11）

不同问题类型应有不同策略：

| 问题类型 | 策略 | 示例 |
|----------|------|------|
| 简单事实 | 单轮搜索，快速返回 | "Python 3.13 发布时间" |
| 对比分析 | 分别搜索，需要多源交叉 | "对比 A 和 B" |
| 开放探索 | 多轮迭代，逐步深入 | "AI 对教育的影响" |
| 深度调研 | 多源 + 多角度 + 学术来源 | "XX 技术的安全漏洞分析" |

不需要完美分类，大模型判断即可，但必须有这个意识。

#### 关于交叉验证（F4）

示例：

    来源A说："XX公司融资1亿美元"
    来源B说："XX公司融资1亿美元"
    来源C说："XX公司融资8000万美元"

    → 报告中应写：
      "XX公司完成新一轮融资，金额存在争议：
       来源A[1]和来源B[2]报道为1亿美元，
       来源C[3]报道为8000万美元，
       具体金额待官方确认。"

#### 关于自校验（F6）

生成回答后，至少检查：

1. 回答中的 [1][2][3] 是否真的对应源文档中的内容（不能张冠李戴）
2. 用户问题的所有方面是否都被覆盖（不能答非所问）
3. 如果检查不通过，修正后重新输出

---

## 五、回答输出格式（参考模板）

> 以下是参考格式，开发者可以优化调整，但 **"引用溯源"和"结构化输出"** 两个要素必须保留。

    # [问题/主题]

    > 生成时间：2025-07-11 14:30
    > 搜索轮次：2 轮 | 有效来源：8 个 | 抓取成功：6 个

    ## 摘要

    [2-3句话概括核心结论]

    ## 详细分析

    ### 1. [要点标题]

    [详细内容...]

    > 来源：
    > [1] 文章标题 — https://example.com/article1
    > [2] 文章标题 — https://example.com/article2

    **可信度**：✅ 多源验证（3个来源提及）

    ### 2. [要点标题]

    [详细内容...]

    > 来源：
    > [3] 文章标题 — https://example.com/article3

    **可信度**：⚠️ 单一来源，建议进一步确认

    ## 存在争议的信息

    - 来源[1]称"..."，但来源[4]称"..."，两者矛盾，目前无法确认。

    ## 全部来源

    | 编号 | 标题 | 链接 | 状态 |
    |------|------|------|------|
    | [1]  | ...  | ...  | ✅ 正文抓取成功 |
    | [4]  | ...  | ...  | ⚠️ 仅使用搜索摘要 |

---

## 六、体验要求

### 核心原则：不是"快"，而是"用户不焦虑"

| 要求 | 说明 |
|------|------|
| 首次反馈 < 3秒 | 用户输入后，3秒内必须有第一个反馈（如"正在分析问题..."），不能是空白等待 |
| 全程进度可见 | 用户能实时看到当前在哪一步：在搜什么、抓了几个页面、在生成什么 |
| 流式输出 | 最终回答逐字/逐段输出，不要等全部生成完再一次性展示 |
| 耗时预期管理 | 简单问题目标30秒内，复杂问题目标90秒内。如果预计超时，提前告知用户 |
| 单步超时不阻塞 | 单个网页抓取超时→跳过；大模型调用超时→重试一次→仍失败则降级 |
| 失败有提示 | 任何环节失败，给出明确的错误信息，不静默失败 |

### 关于"大模型慢"的应对

大模型调用客观需要时间，不需要回避。通过以下方式缓解：

- 流式输出（用户第5秒就开始看到内容）
- 进度提示（用户知道系统在干什么）
- 并发执行（多路搜索和抓取并行，不串行）
- 简单问题走轻量流程（减少大模型调用次数）

---

## 七、技术约束

> 以下是建议和边界，不是强制方案。开发者可根据实际情况调整，但需在文档中说明理由。

### 建议

| 方面 | 建议 |
|------|------|
| 语言 | Python 3.11+ |
| 大模型 | OpenAI 兼容接口（可对接 OpenAI/DeepSeek/通义/本地模型），通过环境变量配置 |
| 网页搜索 | 优先免费方案，必须有降级策略 |
| 网页抓取 | 处理超时、重试、User-Agent、常见反爬 |
| 并发 | 多路搜索和多页抓取必须并发，不能串行 |
| 数据存储 | 本地文件系统（Markdown + 配置文件），不需要数据库 |
| 交互方式 | CLI 命令行 |
| 定时任务 | 轻量方案即可 |
| 配置管理 | 配置文件（YAML/JSON），与代码分离 |

### 硬性约束（必须遵守）

1. **项目必须在没有付费 API Key 的情况下也能运行**。大模型支持 Mock 模式（预设模拟回答），搜索有降级方案。保证流程可跑通、可测试
2. **所有网络请求必须有超时和异常处理**，单个请求失败不能导致整个流程崩溃
3. **不要使用 LangChain、LlamaIndex 等重型框架**，自己实现核心逻辑
4. **Prompt 模板集中管理**，不要硬编码在函数里，方便调整
5. **生成的回答必须保存为本地文件**，不能只打印到终端
6. **代码必须可重复运行**，同样的输入应产生结构一致的输出

### 架构扩展性要求（不增加当前复杂度，但不堵死未来）

虽然当前不实现以下功能，但代码结构上请不要把路堵死：

| 扩展方向 | 当前要求 |
|----------|----------|
| 新增搜索源 | 新增一个搜索源时，不应需要修改核心逻辑，只需新增一个实现 |
| 输出格式 | 当前输出 Markdown，未来可能输出其他格式。输出模块与生成逻辑解耦 |
| 存储方式 | 当前用文件系统，未来可能引入向量数据库。存储层有抽象，不要到处直接 open() |
| 搜索策略 | 当前是通用策略，未来可能针对不同领域有不同策略。策略应可配置 |

---

## 八、质量要求

### 必须做到

- 核心流程（搜索→抓取→整理→生成）有单元测试
- 使用 `logging` 记录关键步骤（搜了什么、抓了几个页面、迭代了几轮、生成了什么）
- 所有外部调用（搜索、抓取、大模型）有异常处理和降级方案
- 代码可读，函数职责单一，不出现巨型函数
- 关键逻辑有注释

### 不需要做到

- 不需要 100% 测试覆盖率
- 不需要性能压测
- 不需要安全审计
- 不需要 API 文档
- 不需要 CI/CD

---

## 九、面试视角的设计亮点

> 以下是本项目区别于"调一次搜索 API + 让大模型总结"的核心差异。请在实现中重点打磨，这些是面试时最值得深聊的部分。

| 亮点 | 说明 | 面试时怎么聊 |
|------|------|-------------|
| **迭代式搜索** | 不是搜一次就完，是"搜→评估→调整→再搜"的循环 | "我的系统有停止条件判断，不是搜一轮就硬生成" |
| **策略自适应** | 不同问题类型走不同策略 | "简单事实查询和开放探索用完全不同的搜索策略" |
| **交叉验证** | 多源验证、矛盾检测、可信度标注 | "我解决了大模型幻觉问题，不依赖单一来源" |
| **答案自校验** | 生成后检查引用准确性和覆盖完整性 | "不是生成完就给用户，有质量把关环节" |
| **过程可观测** | 用户能看到系统的"思考过程" | "这既是产品体验，也是调试手段" |

---

## 十、项目演进规划（历史基线与当前状态）

### 原始版本规划（V1，历史）

- 核心：问→搜→答，带引用的深度搜索
- 附加：多轮追问、定时收集
- 定位：个人每日使用的信息获取工具

### 当前实现（2.1.0）

- 核心：F1–F16 已实现，研究循环位于 `application/ResearchService`；
- 架构：domain/application/infrastructure/presentation/scheduling 五个职责层，由 `bootstrap.py` 组合；
- 交付：CLI、五页面 Streamlit 工作台、结论优先总结、结构化轨迹、五维评分、Markdown/Text/JSON 报告；
- 自主任务：每日/每周/单次周期，领域、信息类型、时间范围、篇幅、读者、章节与模板要求进入实际研究流程；
- 可靠性：在线双搜索源、Mock 降级、超时与异常隔离、两级缓存、引用质量门；
- 验证：24 项离线优先回归测试通过；生产级语义蕴含、后台任务队列和跨报告知识库尚未实现。

### 原规划的未来方向（重新基线为后续版本）

| 版本 | 方向 | 描述 |
|------|------|------|
| V3 | 知识积累 | 搜索结果不只是保存为文件，逐步构建个人知识库。下次搜索时优先从已有知识中检索，再补充互联网信息 |
| V3 | 跨报告关联 | 发现不同时间的报告之间的关联，主动提示用户 |
| V3+ | 主动监控 | 不是"我问它才搜"，而是"它发现新信息主动告诉我" |
| V3+ | 多模态输出 | 报告中生成对比图表、时间线等可视化内容 |

**当前 2.1 架构已为这些方向保留端口和存储扩展点，但尚未把它们标记为已完成。**

---

## 十一、收尾工作

### 11.1 原始验收清单

> 当前逐项验收结果已迁移到 `docs/REQUIREMENTS_TRACEABILITY.md`，这里保留最初的检查口径，避免历史需求与实现证据混在一起。

项目完成后，逐项确认：

- [ ] 在干净环境中安装依赖成功
- [ ] 输入一个简单问题（如"Python 3.13 什么时候发布"），15秒内返回带引用的回答
- [ ] 输入一个复杂问题（如"对比2025年主流大模型的推理能力与成本"），系统能拆解、多轮搜索、生成对比报告
- [ ] 复杂问题搜索过程中，能看到迭代搜索的日志（"信息不够，正在补充搜索..."）
- [ ] 生成的报告格式清晰，引用可追溯到具体来源
- [ ] 矛盾信息被标注
- [ ] 追问功能正常
- [ ] 报告保存为本地 Markdown 文件
- [ ] 搜索源不可用时，程序不崩溃，给出友好提示
- [ ] 大模型不可用时，程序不崩溃，给出友好提示
- [ ] 断网时运行，不崩溃，有明确错误提示
- [ ] 运行测试，全部通过
- [ ] 定时收集功能正常（如已实现）

### 11.2 必须输出的文档

#### README.md

    1. 项目简介（一段话说清楚这是什么、解决什么问题）
    2. 核心特性（迭代搜索、交叉验证、引用溯源、策略自适应）
    3. 快速开始（安装→配置→第一次使用的完整步骤）
    4. 配置说明（大模型配置、搜索源配置、主题配置）
    5. 使用示例（至少2个：一个简单问题、一个复杂问题的完整输入输出）
    6. 项目结构说明
    7. 核心设计决策（为什么这样设计，关键技术选型的理由）
    8. 已知限制与后续改进方向

#### docs/USER_GUIDE.md

    面向使用者的操作手册：
    - 如何提问（命令格式）
    - 如何追问
    - 如何配置定时任务
    - 如何查看历史报告
    - 常见问题排查（API Key、网络、超时等）

#### docs/DEV_NOTES.md

    开发者自己的记录：
    - 关键技术决策及理由（为什么选这个方案而不是那个）
    - 迭代搜索的停止条件是怎么设计的
    - 交叉验证的具体实现逻辑
    - 遇到的难点和解决方案
    - 哪些地方做了简化/妥协
    - 如果要继续迭代，建议从哪里入手

### 11.3 最终验证

完成所有开发后，执行一次端到端验证：

    1. 简单问题测试：
       输入 "Python 3.13 有哪些新特性"
       → 确认：快速返回，带引用，保存为文件

    2. 复杂问题测试：
       输入 "对比 2025 年主流大模型的推理能力和成本"
       → 确认：问题被拆解，多轮搜索，有对比表格，引用完整

    3. 迭代搜索验证：
       检查日志，确认复杂问题触发了至少2轮搜索
       → 确认：日志中有"信息不够，正在补充搜索"类似记录

    4. 追问测试：
       基于上述回答追问 "第一个模型的最新版本有什么变化"
       → 确认：能基于已有内容回答，必要时追加搜索

    5. 异常测试：
       断网后运行 → 确认不崩溃，有友好提示
       配置错误的 API Key → 确认不崩溃，有明确错误信息

    6. 运行测试：
       执行所有单元测试，确认通过

    7. 检查生成的报告文件，格式是否符合第五节要求

---

## 十二、给开发者的话

1. **核心是"深度"二字。** 不是搜得快，是搜得深、读得细、验证得严、写得好。把这个体验做好，项目就成了。

2. **迭代搜索是灵魂。** 这是本项目区别于"调一次搜索API+让大模型总结"的根本。请在"信息充分性评估"和"策略调整"上花最多心思。

3. **回答质量是第一优先级。** 技术方案再优雅，如果生成的回答没有信息量、引用不准确、结构混乱，项目就是失败的。

4. **你有完全的自主权。** 架构、代码结构、具体技术选型、实现细节全部由你决定。这份文档只告诉你"我要什么体验"。如果你认为有更好的方案，请调整，并在 `DEV_NOTES.md` 中说明理由。

5. **先跑通，再打磨。** 第一优先级是端到端流程走通。不要在某个细节上过度打磨而忽略整体。

6. **保持简单。** 这是个人工具，不是企业级平台。不需要过度设计。能用文件解决的不用数据库，能用单进程解决的不用分布式。

7. **Mock 不是作弊。** 如果真实的搜索/抓取/大模型在当前环境不可用，设计 Mock 层让流程跑通，但要预留真实接口的扩展点，并在代码中用 `TODO` 标注。

8. **报告模板可以调整。** 第五节的模板是参考，你可以根据实际效果优化，但"引用溯源"和"结构化输出"不能丢。

9. **想想你自己会不会每天用它。** 如果做完之后你自己都不会打开用，那说明体验还不够好。启动成本要低（一条命令），等待体验要好（有进度），输出质量要稳（不是时好时坏）。
